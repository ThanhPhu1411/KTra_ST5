@model List<KTra_ST5.Models.Product>

<div class="container mt-4">
<<<<<<< HEAD

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-primary">🛍 Danh sách sản phẩm</h2>

        <a class="btn btn-success px-4" href="/Product/Create">
            ➕ Thêm sản phẩm
        </a>
    </div>

    <div class="row g-4">

        @foreach (var item in Model)
        {
            <div class="col-md-3 col-sm-6">
                <div class="card shadow-sm rounded-4 h-100">

                    <img src="/images/@item.Image"
                         class="card-img-top"
                         style="height: 180px; object-fit: cover; border-top-left-radius: 12px; border-top-right-radius: 12px;" />

                    <div class="card-body">
                        <h5 class="card-title fw-bold">@item.Name</h5>
                        <p class="text-success fw-semibold">@String.Format("{0:N0} đ", item.Price)</p>

                        <!-- 3 nút nằm ngang đều nhau -->
                        <div class="d-flex gap-2">
                            <a href="/Product/Details/@item.Id" class="btn btn-primary btn-sm w-100">
                                👁 Xem
                            </a>

                            <a href="/Product/Edit/@item.Id" class="btn btn-warning btn-sm w-100">
                                ✏ Sửa
                            </a>

                            <a href="/Product/Delete/@item.Id"
                               class="btn btn-danger btn-sm w-100"
                               onclick="return confirm('Bạn có chắc muốn xóa sản phẩm này không?');">
                                🗑 Xóa
                            </a>
                        </div>
                    </div>

                </div>
            </div>
        }

    </div>
=======
    <h2 class="mb-3">Danh sách sản phẩm</h2>

    <a class="btn btn-primary mb-3" href="/Product/Create">Thêm sản phẩm</a>

    <table class="table table-bordered table-striped">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Price</th>
                <th>Image</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>@item.Price</td>
                    <td>
                        <img src="/images/@item.Image" width="80" />
                    </td>
                    <td>
                        <a href="/Product/Edit/@item.Id" class="btn btn-warning btn-sm">Sửa</a>
                        <a href="/Product/Delete/@item.Id" class="btn btn-danger btn-sm">Xóa</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
>>>>>>> remotes/origin/update-price-B
</div>
